#!/usr/bin/env bash
set -euo pipefail

branch="${1:-$(git symbolic-ref --quiet --short HEAD 2>/dev/null || true)}"
case "${branch}" in
  master) echo normal ;;
  release/*) echo hotfix ;;
  *) echo "Unsupported branch for release mode: ${branch}" >&2; exit 1 ;;
esac
